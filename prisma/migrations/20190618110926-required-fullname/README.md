# Migration `20190618110926-required-fullname`

This migration has been generated by timsuchanek at 6/18/2019, 11:09:26 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "file:dev"."new_User"("id" INTEGER NOT NULL DEFAULT 0 ,"firstName" TEXT NOT NULL DEFAULT '' ,"lastName" TEXT NOT NULL DEFAULT '' ,"fullName" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

INSERT INTO "new_User" ("id","firstName","lastName","fullName") SELECT "id","firstName","lastName","fullName" from "User"

DROP TABLE "file:dev"."User";

ALTER TABLE "file:dev"."new_User" RENAME TO "User";

PRAGMA "file:dev".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190618110220-optional-fullname..20190618110926-required-fullname
--- datamodel.dml
+++ datamodel.dml
@@ -11,6 +11,7 @@
 model User {
   id Int @id
   firstName String
   lastName String
-  fullName String?
-}
+  fullName String
+}
+
```

## Photon Usage

You can use a specific Photon built for this migration (20190618110926-required-fullname)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190618110926-required-fullname'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
