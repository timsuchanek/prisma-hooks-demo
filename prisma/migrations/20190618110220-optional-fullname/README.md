# Migration `20190618110220-optional-fullname`

This migration has been generated by timsuchanek at 6/18/2019, 11:02:20 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "file:dev"."User" ADD COLUMN "fullName" TEXT   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190618110142-init..20190618110220-optional-fullname
--- datamodel.dml
+++ datamodel.dml
@@ -11,5 +11,6 @@
 model User {
   id Int @id
   firstName String
   lastName String
+  fullName String?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190618110220-optional-fullname)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190618110220-optional-fullname'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
